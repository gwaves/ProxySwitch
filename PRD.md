# ProxySwitch - macOS 代理管理工具 PRD

## 1. 项目概述

### 1.1 产品名称
ProxySwitch

### 1.2 一句话描述
macOS 菜单栏代理管理工具，一键切换/开关系统代理与终端代理。

### 1.3 问题陈述
macOS 上代理配置分散在多个位置（系统网络设置、Shell 环境变量），每次切换代理需要分别操作，流程繁琐且容易遗漏。现有方案（如系统偏好设置）操作路径深，终端代理更是需要手动编辑 shell 配置。

### 1.4 目标用户
- 开发者（频繁切换代理环境）
- 需要科学上网的用户
- 在多个代理配置间切换的用户

---

## 2. 功能需求

### 2.1 P0 - 核心功能

#### 2.1.1 菜单栏常驻
- App 启动后仅在菜单栏显示图标，无 Dock 图标（可配置）
- 图标状态反映当前代理开关状态
  - 代理开启：彩色/填充图标
  - 代理关闭：灰色/空心图标
- 点击图标弹出主面板

#### 2.1.2 代理开关
- 一键开启/关闭系统网络代理（HTTP/HTTPS/SOCKS）
- 一键开启/关闭终端代理（zsh 环境变量）
- 提供"全部开启"和"全部关闭"快捷操作
- 开关状态实时反映在 UI 上

#### 2.1.3 代理配置管理
- 支持添加/编辑/删除多个代理配置
- 每个代理配置包含：
  - 名称（如"公司代理"、"香港节点"）
  - 代理类型：HTTP / SOCKS5
  - 地址（IP 或域名）
  - 端口
  - 例外列表（bypass domains，可选）
- 代理配置支持导入/导出（JSON 格式）

#### 2.1.4 代理快速切换
- 主面板列出所有已保存的代理配置
- 点击配置项即切换到该代理
- 当前激活的配置高亮显示

#### 2.1.5 系统代理设置
- 通过 `networksetup` 命令修改系统网络代理
- 支持的网络服务：Wi-Fi、以太网（自动检测活跃网卡）
- 设置项：
  - HTTP 代理（Web Proxy）
  - HTTPS 代理（Secure Web Proxy）
  - SOCKS 代理（SOCKS Proxy）
  - 例外主机（Bypass Domains）

#### 2.1.6 终端代理设置
- 向 `~/.zshrc` 或 `~/.zprofile` 写入/移除代理环境变量
- 设置的环境变量：
  - `http_proxy`
  - `https_proxy`
  - `ALL_PROXY`
  - `no_proxy`
- 开启时追加 export 语句，关闭时移除
- 提供"仅当前终端会话"模式（不写文件，通过 osascript 通知已打开终端）

### 2.2 P1 - 增强功能

#### 2.2.1 开机自启
- 支持设置开机自动启动（Login Item）

#### 2.2.2 代理自动检测
- 定期检测当前代理是否可达（TCP connect 测试）
- 代理不可达时图标变红/显示警告
- 检测间隔可配置（默认 30 秒）

#### 2.2.3 快捷键
- 全局快捷键快速开关代理
- 默认快捷键：`⌘ + Shift + P` 开关代理

#### 2.2.4 代理模式
- 自动配置（PAC）模式支持
- 直连模式（临时关闭所有代理但不丢失配置）

### 2.3 P2 - 未来迭代

- 代理流量统计
- 按应用设置代理（proxy per app）
- 订阅链接导入（Shadowsocks/V2Ray 格式）
- iCloud 同步代理配置

---

## 3. 技术方案

### 3.1 技术栈
- **语言**: Swift
- **UI 框架**: SwiftUI
- **最低版本**: macOS 13.0 (Ventura)
- **架构**: Menu Bar App（NSStatusBar + SwiftUI Popover / Window）

### 3.2 系统代理实现
```
networksetup -setwebproxy <networkservice> <domain> <port>
networksetup -setsecurewebproxy <networkservice> <domain> <port>
networksetup -setsocksfirewallproxy <networkservice> <domain> <port>
networksetup -setwebproxystate <networkservice> <on|off>
networksetup -setsecurewebproxystate <networkservice> <on|off>
networksetup -setsocksfirewallproxystate <networkservice> <on|off>
```

### 3.3 终端代理实现
- 写入模式：编辑 `~/.zshrc`，使用标记注释界定代理代码块
  ```bash
  # >>> ProxySwitch >>>
  export http_proxy="http://127.0.0.1:7890"
  export https_proxy="http://127.0.0.1:7890"
  export ALL_PROXY="socks5://127.0.0.1:7891"
  export no_proxy="localhost,127.0.0.1,::1"
  # <<< ProxySwitch <<<
  ```
- 关闭时移除标记块内内容
- 需要处理文件不存在、无写权限等边界情况

### 3.4 数据存储
- 代理配置存储：`~/Library/Application Support/ProxySwitch/config.json`
- App 设置存储：`UserDefaults` / `@AppStorage`
- 配置文件结构：
  ```json
  {
    "profiles": [
      {
        "id": "uuid",
        "name": "香港节点",
        "type": "http",
        "host": "127.0.0.1",
        "httpPort": 7890,
        "socksPort": 7891,
        "bypass": ["localhost", "127.0.0.1", "*.local"]
      }
    ],
    "activeProfileId": "uuid",
    "systemProxyEnabled": true,
    "terminalProxyEnabled": true
  }
  ```

### 3.5 权限需求
- 系统代理修改需要管理员权限（`networksetup` 需 sudo 或通过 `AuthorizationExecuteWithPrivileges`）
- 实际方案：`networksetup` 修改代理不需要 sudo，只有修改网络接口等才需要

---

## 4. 用户体验

### 4.1 交互流程

**首次启动流程：**
1. 引导用户添加第一个代理配置
2. 检测当前系统网络服务（Wi-Fi/以太网）
3. 请求辅助功能权限（快捷键支持）

**日常使用流程：**
1. 点击菜单栏图标
2. 弹出面板显示当前状态 + 代理列表
3. 点击切换 / 开关
4. 面板关闭，图标状态更新

### 4.2 状态流转

```
[无配置] --添加配置--> [有配置/代理关] --开启--> [代理开]
[代理开] --关闭--> [有配置/代理关]
[代理开] --切换配置--> [代理开(新配置)]
[代理开] --代理不可达--> [代理开(警告)]
```

---

## 5. 非功能需求

| 项目 | 要求 |
|------|------|
| 启动时间 | < 1s |
| 内存占用 | < 50MB |
| CPU 占用 | 空闲时 0%，检测时 < 1% |
| 崩溃率 | < 0.1% |
| 兼容性 | macOS 13+ |
| 安全性 | 代理配置本地加密存储，不传输到外部 |

---

## 6. 里程碑

| 阶段 | 内容 | 预计周期 |
|------|------|----------|
| M1 | P0 功能：菜单栏 + 开关 + 配置管理 + 系统代理 | 2 周 |
| M2 | P0 功能：终端代理 + 快速切换 | 1 周 |
| M3 | P1 功能：自启动 + 健康检测 + 快捷键 | 1 周 |
| M4 | 打包分发 + 测试 | 1 周 |

---

## 7. 原型设计

### 7.1 菜单栏图标状态

```
代理开启:    ◉ (实心，主题色)
代理关闭:    ○ (空心，灰色)
代理异常:    ◉ (实心，红色)
```

### 7.2 主面板（Popover）

```
┌─────────────────────────────────┐
│  ProxySwitch              ⚙ ⨯  │
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────┐    │
│  │ 系统代理    [  ON  ]    │    │  ← 开关组件
│  │ 终端代理    [  ON  ]    │    │
│  └─────────────────────────┘    │
│                                 │
│  ─── 代理配置 ─────────────────  │
│                                 │
│  ● 香港节点                     │  ← 当前选中，高亮
│    HTTP 127.0.0.1:7890          │
│    ● 已连接                     │
│                                 │
│  ○ 公司代理                     │
│    SOCKS5 10.0.0.1:1080         │
│                                 │
│  ○ 日本节点                     │
│    HTTP 192.168.1.1:8080        │
│                                 │
│  ─────────────────────────────  │
│  ＋ 添加代理配置                 │
│                                 │
└─────────────────────────────────┘
```

### 7.3 代理编辑界面

```
┌─────────────────────────────────┐
│  ← 添加代理配置                  │
├─────────────────────────────────┤
│                                 │
│  名称                           │
│  ┌─────────────────────────┐    │
│  │ 香港节点                 │    │
│  └─────────────────────────┘    │
│                                 │
│  代理类型                       │
│  ┌────────┐ ┌────────┐          │
│  │ HTTP   │ │ SOCKS5 │          │  ← Segmented Control
│  └────────┘ └────────┘          │
│                                 │
│  HTTP 代理地址                   │
│  ┌───────────────┐ ┌──────┐     │
│  │ 127.0.0.1     │ │ 7890 │     │
│  └───────────────┘ └──────┘     │
│  地址               端口        │
│                                 │
│  SOCKS5 代理地址（可选）         │
│  ┌───────────────┐ ┌──────┐     │
│  │ 127.0.0.1     │ │ 7891 │     │
│  └───────────────┘ └──────┘     │
│                                 │
│  例外主机（Bypass）              │
│  ┌─────────────────────────┐    │
│  │ localhost, 127.0.0.1     │    │
│  │ *.local, *.lan          │    │
│  └─────────────────────────┘    │
│                                 │
│  ┌─────────────────────────┐    │
│  │        保  存            │    │
│  └─────────────────────────┘    │
│                                 │
└─────────────────────────────────┘
```

### 7.4 设置界面

```
┌─────────────────────────────────┐
│  ← 设置                         │
├─────────────────────────────────┤
│                                 │
│  通用                           │
│  ├ 开机自启         [ OFF ]     │
│  ├ 显示 Dock 图标   [ OFF ]     │
│  └ 全局快捷键  ⌘⇧P  [录制]     │
│                                 │
│  代理检测                       │
│  ├ 自动检测代理可用性 [ ON ]     │
│  ├ 检测间隔         [ 30s ▾]   │
│  └ 检测目标    [ https://www.google.com ] │
│                                 │
│  终端代理                       │
│  ├ 配置文件  [ ~/.zshrc ▾ ]     │
│  └ 生效方式  [ 写入文件 ▾ ]     │
│                                 │
│  网络                           │
│  ├ 活跃网络服务: Wi-Fi          │
│  └ 自动检测网络服务   [ ON ]     │
│                                 │
│  ─────────────────────────────  │
│  关于 ProxySwitch    v1.0.0     │
│  导出配置   导入配置   重置     │
│                                 │
└─────────────────────────────────┘
```

### 7.5 交互细节

| 操作 | 触发 | 反馈 |
|------|------|------|
| 点击菜单栏图标 | 左键单击 | 弹出/关闭主面板 |
| 右键菜单栏图标 | 右键单击 | 退出 / 关于 / 设置 |
| 点击代理配置项 | 单击列表项 | 切换到该代理，面板保持打开 |
| 删除代理配置 | 左滑或右键菜单 | 确认弹窗 → 删除 |
| 编辑代理配置 | 右键菜单 → 编辑 | 进入编辑界面 |
| 点击开关 | 单击开关 | 即时切换，图标状态同步更新 |
| 关闭面板 | 点击面板外区域 | 面板收起 |

### 7.6 右键菜单

```
┌──────────────────┐
│ 开启全部代理      │
│ 关闭全部代理      │
│ ──────────────── │
│ 上次使用的配置    │
│ ──────────────── │
│ 设置...          │
│ 关于 ProxySwitch  │
│ 退出             │
└──────────────────┘
```

---

## 8. 开放问题

1. **权限方案**：`networksetup` 修改 HTTP/SOCKS 代理不需要 sudo，但某些企业环境可能限制，是否需要考虑？
2. **终端即时生效**：写入 .zshrc 后需要 `source` 才能在已打开终端生效，是否通过 AppleScript 通知用户手动 source？
3. **多网卡场景**：同时连接 Wi-Fi 和 USB 以太网时，是否同时设置两者的代理？
4. **与代理客户端联动**：是否需要自动检测本地运行的代理客户端（Clash/V2Ray）的端口？
